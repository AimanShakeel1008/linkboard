# Phase 0 playground: files regenerated by make_logs.sh — history doesn't need them
concepts/phase-00-tools-of-the-trade/playground/server.log
concepts/phase-00-tools-of-the-trade/playground/anonymized.log

# Python
__pycache__/
*.pyc
.venv/
venv/

# Phase 3-4 runtime artifacts: generated when you run the app/demos
project/links.json       # LinkBoard v1's JSON "database" — regenerated from SEED
project/jar.txt          # curl cookie jar from the Phase 3 /count demo

# Environment / secrets (Phase 11 will use a real API key — never commit it)
.env

# OS noise
Thumbs.db
.DS_Store

# Editors
.vscode/
.idea/

CLAUDE.md
curriculum.md
PROGRESS.md
